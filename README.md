# User List App – PHP CRUD Application

A **PHP-based CRUD (Create, Read, Update, Delete) application** that manages user records efficiently using **MySQL** and **AJAX**.  
This project demonstrates core backend development concepts using **raw PHP and SQL** without frameworks.

---

## 🔥 Key Features
- Add new user records
- Edit existing users
- Delete users
- Fetch users dynamically
- AJAX-based operations (no page reload)
- Clean and simple UI

---

## 🧰 Tech Stack
- **Backend:** PHP (Procedural)
- **Database:** MySQL (SQL)
- **Frontend:** HTML, CSS, JavaScript
- **Server:** Apache (XAMPP)

---
